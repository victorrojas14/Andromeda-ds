/*
 * Code Connect — Grid (React)
 *
 * BLOQUEADO: el fundamento "Grid" del archivo Figma "Ui Kit Web" es un
 * frame de documentación (node 3389:96), no un componente publicado en
 * la librería, y Figma solo permite Code Connect sobre componentes
 * publicados (lo mismo aplica al mapeo vía MCP: "Published component
 * not found").
 *
 * Cuando el equipo de diseño publique un componente/component set Grid,
 * descomentá este bloque y reemplazá el node-id por el del componente:
 *
 * import figma from '@figma/code-connect'
 * import { Grid, GridCol } from './Grid'
 *
 * figma.connect(
 *   Grid,
 *   'https://www.figma.com/design/oTZzdsgGkCjbL2f3oybxD0/Ui-Kit-Web?node-id=XXX-XXX',
 *   {
 *     example: () => (
 *       <Grid>
 *         <GridCol span={8}>Contenido principal</GridCol>
 *         <GridCol span={4}>Sidebar</GridCol>
 *       </Grid>
 *     ),
 *   },
 * )
 */

export {}
